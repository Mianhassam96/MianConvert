const Footer = () => (
  <footer className="bg-gray-900 text-gray-400 py-6 text-center text-sm mt-auto">
    <p className="font-semibold text-white mb-1">MianConvert</p>
    <p>Convert, mute, extract — all in your browser. No uploads, no servers.</p>
    <p className="mt-2 text-gray-600">© {new Date().getFullYear()} MianConvert. All rights reserved.</p>
  </footer>
);

export default Footer;
